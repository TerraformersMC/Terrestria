base.archivesName = "terrestria-common"

fabricApi {
	configureDataGeneration {
		client = true
	}
}

dependencies {
	// Biolith for BiomePerimeters in the VolcanoStructure
	implementation "com.terraformersmc:biolith-fabric:${biolith_version}"
}
